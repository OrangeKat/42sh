AS_ARG:
    .step1:
        - not existant 1 and 2
        - two inputs
        - command spaces

AS_INPUT:
    .many_commands
    .no space
    .spaces
    .if commands
    .single quote bad keyword
    .hard pipe 

ask what script is
